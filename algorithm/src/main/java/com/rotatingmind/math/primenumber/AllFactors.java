package com.rotatingmind.math.primenumber;

public class AllFactors {

    /**
     * num = p1^c1 * p2^c2...
     * div = p1^i*p2^j*...
     *
     * count = we have c1 + 1 choises for i and c2+1 choices for j and so on so forth
     *       = c1 + 1 + c2 + 1
     *       = c1+ c2+ 2;
     * @param n
     */
    public static void printFactor(int n) {

    }
}
