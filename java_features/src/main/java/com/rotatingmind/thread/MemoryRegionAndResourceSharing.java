package com.rotatingmind.thread;

/**
 * What is Stack:
 * Stack frame contains argument once thread allocate get memory
 * Heap is shared memory while stack in tread based memory
 * Heap can be shared through out the current process
 * ------------------------------------------------------------
 * Why do we want to share resource:
 * dispatcher shared work among all other worker thread
 * -> connection of db can be shared among the all request response
 * request thread
 */
public class MemoryRegionAndResourceSharing {
}


